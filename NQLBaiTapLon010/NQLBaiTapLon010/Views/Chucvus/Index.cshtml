@model IEnumerable<NQLBaiTapLon010.Models.Chucvu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tenchucvu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tenchucvu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDchucvu }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDchucvu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDchucvu })
        </td>
    </tr>
}

</table>
